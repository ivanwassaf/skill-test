# Test Environment Configuration
NODE_ENV=test

# Database (Separate test database)
DATABASE_URL=postgresql://postgres@127.0.0.1:5433/school_mgmt_test

# JWT Secrets (Different from production)
JWT_ACCESS_TOKEN_SECRET=test_access_secret_key_for_testing_only
JWT_REFRESH_TOKEN_SECRET=test_refresh_secret_key_for_testing_only
JWT_ACCESS_TOKEN_TIME_IN_MS=900000
JWT_REFRESH_TOKEN_TIME_IN_MS=604800000

# CSRF Protection
CSRF_TOKEN_SECRET=test_csrf_secret_key_for_testing_only_32chars
CSRF_TOKEN_TIME_IN_MS=950000

# Email Configuration (Mocked in tests)
MAIL_FROM_USER=test@schooltest.com
RESEND_API_KEY=re_test_mock_key

# Email Verification
EMAIL_VERIFICATION_TOKEN_SECRET=test_email_verification_secret_key
EMAIL_VERIFICATION_TOKEN_TIME_IN_MS=18000000

# Password Setup
PASSWORD_SETUP_TOKEN_SECRET=test_password_setup_secret_key
PASSWORD_SETUP_TOKEN_TIME_IN_MS=18000000

# URLs
UI_URL=http://localhost:3000
API_URL=http://localhost:5007
COOKIE_DOMAIN=localhost

# Server
PORT=5007

# Redis (Disabled in tests)
REDIS_ENABLED=false
REDIS_URL=redis://localhost:6379

# Blockchain (Disabled in tests)
BLOCKCHAIN_NETWORK=localhost
BLOCKCHAIN_PRIVATE_KEY=
BLOCKCHAIN_CONTRACT_ADDRESS=

# IPFS (Disabled in tests)
PINATA_API_KEY=
PINATA_SECRET_KEY=
